# Postgres connection string for Prisma.
# The `?schema=ukrstene` query param isolates this app's tables in a dedicated
# Postgres schema, so in production they can live alongside another app's
# tables in the same database without collisions.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=ukrstene"
